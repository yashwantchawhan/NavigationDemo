<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/fragment_graph"
    app:startDestination="@id/fragmentHome">

    <fragment
        android:id="@+id/fragmentHome"
        android:name="com.example.navigationdemo.FragmentHomeScreen"
        android:label="Fragment Home"
        tools:layout="@layout/fragment_home">

        <action
            android:id="@+id/action_home_to_details"
            app:destination="@id/fragmentDetails" />
    </fragment>

    <fragment
        android:id="@+id/fragmentDetails"
        android:name="com.example.navigationdemo.FragmentDetailsScreen"
        android:label="Fragment Details"
        tools:layout="@layout/fragment_details">

        <argument
            android:name="orderId"
            app:argType="string"
            android:defaultValue="" />
    </fragment>

</navigation>
